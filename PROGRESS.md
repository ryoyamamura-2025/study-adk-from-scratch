# Progress

## 現在地

- **Lesson 0: 完了**
- **Lesson 1: 完了**
- Function Toolを追加し、`FunctionCall → Tool実行 → FunctionResponse → Final Response` がADKのEvent Loop上で流れることを確認済み。
- `McpToolset` を使い、WSL上のADK AgentからWindows上の外部MCP Serverへ接続できている。
- MCP経由でWindowsのプリンター一覧を取得し、`initialize → tools/list → tools/call` の通信も観察済み。

## 次に始めるセクション

**Lesson 2: 未着手**

次のセッションではLesson 2のテーマ選定から開始する。

プロジェクトの到達イメージ上では、Tool / MCPの次段階としてSkillsを候補とする。
